{% extends 'layouts/base.twig' %}

{% block title %}{{ trans('welcome') }}{% endblock %}

{% block content %}
    <div class="bg-white p-6 rounded-lg shadow-lg">
        <h2 class="text-xl font-semibold mb-4">{{ trans('welcome') }}</h2>
        <p class="text-gray-700">{{ trans('lunapeak_first_page') }}</p>

        <h2 class="text-md font-bold mt-2">{{ trans('useful_links') }}:</h2>
        <a href="https://github.com/WhySpice/lunapeak-framework" target="_blank" class="text-md text-blue-500 hover:text-blue-700 transition duration-300">
            <i class="fa-brands fa-github fa-fw"></i>
            GitHub
        </a>

        <h2 class="text-md font-bold mt-2">Language:</h2>
        <a class="text-md text-blue-500 hover:text-blue-700 transition duration-300" href="{{ route('setLocale', {'locale': 'ru'}) }}">Русский</a>
        <br/>
        <a class="text-md text-blue-500 hover:text-blue-700 transition duration-300" href="{{ route('setLocale', {'locale': 'en'}) }}">English</a>
    </div>
{% endblock %}